import React from 'react';

// This component has been disabled to ensure the application exclusively uses
// the hardcoded API key from the configuration. There is no need to prompt
// the user for a key from the "system".
const ApiKeyPrompt: React.FC = () => {
    return null;
};

export default ApiKeyPrompt;
